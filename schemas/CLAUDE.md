# ops/openclaw/schemas

This directory contains JSON Schema contracts for OpenClaw report payloads.

## Use This Directory For

- machine-readable report contracts used by runtime validation and tests

## Working Rules

- Schema changes must stay aligned with runtime emitters and schema tests.
- Prefer additive or clearly versioned contract changes over silent breakage.
