# hooks

Git hook entrypoints for `moltbot`.

Use this subtree for plan and documentation enforcement around this setup repo.
Keep hook behavior deterministic and aligned with the lightweight workflow here.
